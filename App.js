/* @flow weak */

import React from 'react';
import {
  View,
  Text,
  StyleSheet,
} from 'react-native';
import Main from './components/Main';

const App = ({}) => (
  <Main/>
);

export default App;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
